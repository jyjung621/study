<!DOCTYPE html>
<html>

<head>
    <% include partials/head.ejs %>
    <title>Home</title>
</head>

<body class="hold-transition skin-blue sidebar-mini">
    <div class="wrapper">
        <!-- Header -->
        <% include partials/header.ejs %>


        <!-- Content Wrapper. Contains page content -->
        <div class="content-wrapper">
            <!-- Content Header (Page header) -->
            <section class="content-header">
                <h1>
                    Board Project TEST
                    <small>(Board Project TEST)</small>
                </h1>
                <ol class="breadcrumb">
                    <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
                </ol>
            </section>

            <!-- Main content -->
            <section class="content container-fluid">

                <p>HOME PAGE!!</p>
            <% if(is_logined == true) { %>
                    <p><%=nickname%>님 환영합니다.</p>
            <% } else { %>
                    <p>로그인XXX 환영합니다.</p>
            <% } %>
                
                <input type="button" name="goBook" value="가계부 이동" onclick="location.replace('/book/list')" <% if(is_logined == true){ %> disabled="true" <% } %> >&nbsp;&nbsp;
                <input type="button" name="goBoard" value="게시판 이동" onclick="location.replace('/board/list')" <% if(is_logined == true){ %> disabled="true" <% } %> >&nbsp;&nbsp;
                
            <% if(is_logined == true) { %>
                <input type="button" name="goLogout" value="로그아웃" onclick="location.replace('/member/logoutPro')" <% if(grade === 0) { %> disabled="true" <% } %> >&nbsp;&nbsp;
            <% } else { %>
                <input type="button" name="goLogin" value="로그인" onclick="location.replace('/member/loginForm')" <% if(grade === 0) { %> disabled="true" <% } %> >&nbsp;&nbsp;
            <% } %>
                
                    
            <% if(grade === 0) { %>
                <input type="button" value="관리자 로그아웃" onclick="location.replace('/member/logoutPro')">
            <% } %>
                
            <div class="book-list">
                <h3>BookList</h3>
                <%=JSON.stringify(bookList)%>
                <% for(var i in bookList) { %>
                    <p><%=bookList[i].bookName%></p>
                <% } %>
                
                
            </div>


            </section>
            <!-- /.content -->
        </div>
        <!-- /.content-wrapper -->

        <!-- Footer -->
        <% include partials/footer.ejs %>
        <div class="control-sidebar-bg"></div>
    </div>

</body>

</html>